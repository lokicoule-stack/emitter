{
  "name": "@lokiverse/scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "check:size": "node --loader ts-node/esm check-bundle-size.ts",
    "validate:build": "node --loader ts-node/esm validate-build.ts"
  },
  "devDependencies": {
    "@types/node": "latest",
    "chalk": "^5.3.0",
    "gzip-size": "^7.0.0",
    "pretty-bytes": "^6.1.1",
    "ts-node": "latest"
  }
}
